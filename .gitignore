# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
build
dist-ssr
*.local

# Test artifacts
coverage

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env*
.tmp
.cache
.eslintcache

# local research notes
.notes/

# Playwright (e2e) output
test-results/
playwright-report/
playwright/.cache/

.cache/develop-web-feature/

# impeccable: platform-specific engine binary (12MB, darwin-arm64 only).
# The launcher re-downloads it on first run.
.claude/skills/impeccable/scripts/bin/

# Design canvas build output: the seeded page is ~2.8 MB because the canvas editor
# is baked into it. Regenerate from docs/specs/qrcraft-mobile-design/ instead.
qrcraft-react-native.html
